let divide = (a, b) => {
    if (b === 0) {
     new Error('Division by zero is not allowed');
    }
    return a / b;
  };

let square = (x) => {
    return x * x;
  };

let add = (a, b) => {
    return a + b;
  };
